#include<stdio.h>
#include<string.h>

#define MAX_LEN 1000

int main() {
	
	char s1[MAX_LEN], s2[MAX_LEN];
	while(scanf("%s%s", s1, s2)) {
		
		int mLen = strlen(s1);
		int nLen = strlen(s2);
	}
	
	return 0;
}
